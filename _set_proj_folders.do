* ==============================================================================
* set project folders
* ==============================================================================

global data "${proj_root}/data"
global results "${proj_root}/results"
global results_preloading "${results}/01_preloading"
global results_lookup "${results}/02_lookup"
